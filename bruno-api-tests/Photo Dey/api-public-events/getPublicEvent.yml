info:
  name: getPublicEvent
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/api/v1/public/events/:publicCode
  params:
    - name: publicCode
      value: replace-with-event-public-code
      type: path
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
