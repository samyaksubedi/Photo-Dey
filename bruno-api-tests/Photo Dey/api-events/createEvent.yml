info:
  name: createEvent
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/v1/events
  body:
    type: json
    data: |-
      {
        "name": "SD Photos",
        "expectedTotalPhotos": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
