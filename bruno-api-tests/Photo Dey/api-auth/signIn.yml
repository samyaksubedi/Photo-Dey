info:
  name: signIn
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:3000/api/v1/auth/signIn
  body:
    type: json
    data: ' { "email":"samexecodes@gmail.com", "password":"CLEB1197B2" }'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
