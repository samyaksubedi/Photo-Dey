info:
  name: logout-all
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:3000/api/v1/auth/logout-all
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
